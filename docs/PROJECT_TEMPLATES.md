# Project Templates

`/project-templates` exposes versioned semantic defaults: template ID/version, title key, default Learning Mode, module seed, goal suggestions, source policy, analytics config and lifecycle state.

Templates seed Projects but do not own user state permanently. Project customization is persisted/revisioned separately. Frontend may render semantics differently without changing authorization, ownership or economic rules.