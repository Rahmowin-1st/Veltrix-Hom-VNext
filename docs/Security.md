# Security

Security invariants: account ownership on every durable object, scoped vector retrieval, server-side service secrets, bounded upload MIME/size, validated UUIDs, parameterized PostgreSQL access, session expiry/revocation, idempotency replay protection and no private raw content in normal logs. Final classification depends on exact runtime/security gate evidence.
