# Security Report

Required evidence includes cross-account Project/Source isolation, vector ownership filters, server-side secret policy, source validation, parameterized SQL, malformed-ID/error handling, session auth, idempotency replay, source/APK secret scans and no legacy runtime dependency. Live production credential quality is classified separately from deterministic CI.
