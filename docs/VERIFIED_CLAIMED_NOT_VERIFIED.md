# VERIFIED / CLAIMED / NOT VERIFIED

Use only these labels. `VERIFIED` requires execution evidence from the exact tested commit. Source implementation without runtime execution is `CLAIMED / IMPLEMENTED BUT NOT VERIFIED`. Missing live external credential boundaries are `NOT VERIFIED`. Deterministic CI adapters must always be identified as test-only and never imply live-provider verification.
