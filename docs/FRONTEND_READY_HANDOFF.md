# Frontend-Ready Backend Handoff

Backend/app-foundation is frontend-ready only when Source Audit, Final Gate and Runtime Package are GREEN on the same exact commit. Frontend owns final colors, typography, spacing, artwork, gradients/shadows, animation polish and composition.

Consume as authority: `contracts/openapi.yaml`; Home/Personal/Project Workspace v3 DTOs; `Part3AndroidContracts.kt`; ContextCarry revisions; semantic frontend events; Project template/customization semantics.

Never create client authority for XP, Coins, Store price/ownership, avatar ownership/equip, Map eligibility/progression, seasons, account deletion, permissions, Student Model cross-scope access or goal suggestion acceptance.

Handle typed 401/403/409/429/503 and retryability. Render cached STALE/OFFLINE truthfully; do not invent provider success. Exact source ZIP/APKs, `PROVENANCE.txt`, `SHA256SUMS.txt` and evidence directory are the handoff proof.