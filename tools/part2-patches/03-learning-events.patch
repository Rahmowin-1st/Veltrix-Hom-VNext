--- a/server/src/main/kotlin/com/veltrix/hom/vnext/server/LearningRepositories.kt
+++ b/server/src/main/kotlin/com/veltrix/hom/vnext/server/LearningRepositories.kt
@@ -52,7 +52,7 @@
 }
 
 internal fun upsertMistake(c:Connection,accountId:String,projectId:String?,sourceId:String?,topic:String,prompt:String,user:String,expected:String):String{val existing=c.prepareStatement("SELECT id FROM mistake WHERE account_id=?::uuid AND project_id IS NOT DISTINCT FROM ?::uuid AND source_id IS NOT DISTINCT FROM ?::uuid AND topic=? AND prompt=? AND status<>'ARCHIVED' AND deleted_at IS NULL LIMIT 1").use{ps->ps.setString(1,accountId);ps.setString(2,projectId);ps.setString(3,sourceId);ps.setString(4,topic);ps.setString(5,prompt);ps.executeQuery().use{rs->if(rs.next())rs.uuid("id")else null}};if(existing!=null){c.prepareStatement("UPDATE mistake SET user_answer=?,expected_answer=?,occurrence_count=occurrence_count+1,last_seen_at=now(),status='ACTIVE',revision=revision+1 WHERE id=?::uuid").use{ps->ps.setString(1,user);ps.setString(2,expected);ps.setString(3,existing);ps.executeUpdate()};return existing};return c.prepareStatement("INSERT INTO mistake(account_id,project_id,source_id,topic,prompt,user_answer,expected_answer) VALUES (?::uuid,?::uuid,?::uuid,?,?,?,?) RETURNING id").use{ps->ps.setString(1,accountId);ps.setString(2,projectId);ps.setString(3,sourceId);ps.setString(4,topic);ps.setString(5,prompt);ps.setString(6,user);ps.setString(7,expected);ps.executeQuery().use{rs->rs.next();rs.uuid("id")}}}
-internal fun insertActivity(c:Connection,accountId:String,type:String,projectId:String?,objectId:String?,key:String,meaningful:Boolean){c.prepareStatement("INSERT INTO activity_event(account_id,event_type,project_id,object_id,idempotency_key,meaningful) VALUES (?::uuid,?,?::uuid,?,?,?) ON CONFLICT(account_id,idempotency_key) DO NOTHING").use{ps->ps.setString(1,accountId);ps.setString(2,type);ps.setString(3,projectId);ps.setString(4,objectId);ps.setString(5,key);ps.setBoolean(6,meaningful);ps.executeUpdate()}}
+internal fun insertActivity(c:Connection,accountId:String,type:String,projectId:String?,objectId:String?,key:String,meaningful:Boolean){val evidence="{\"producer\":\"part1-domain\",\"eventType\":\"${type.replace("\"","_")}\"}";c.prepareStatement("INSERT INTO activity_event(account_id,event_type,project_id,object_id,idempotency_key,meaningful,evidence) VALUES (?::uuid,?,?::uuid,?,?,?,?::jsonb) ON CONFLICT(account_id,idempotency_key) DO NOTHING").use{ps->ps.setString(1,accountId);ps.setString(2,type);ps.setString(3,projectId);ps.setString(4,objectId);ps.setString(5,key);ps.setBoolean(6,meaningful);ps.setString(7,evidence);ps.executeUpdate()}}
 internal fun jsonArray(values:List<String>):String=values.joinToString(prefix="[",postfix="]"){"\""+it.replace("\\","\\\\").replace("\"","\\\"").replace("\n","\\n")+"\""}
 internal fun parseJsonStringArray(json:String?):List<String>{if(json.isNullOrBlank())return emptyList();return Regex("\\\"((?:\\\\.|[^\\\"])*)\\\"").findAll(json).map{it.groupValues[1].replace("\\n","\n").replace("\\\"","\"").replace("\\\\","\\")}.toList()}
 internal fun citationJson(values:List<CitationResponse>):String="["+values.joinToString(","){"{\"sourceId\":\"${it.sourceId}\",\"sourceVersion\":${it.sourceVersion},\"chunkId\":\"${it.chunkId}\",\"textHash\":\"${it.textHash}\"}"}+"]"
