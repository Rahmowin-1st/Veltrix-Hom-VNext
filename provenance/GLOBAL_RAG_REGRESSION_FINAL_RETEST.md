# Global RAG regression final retest

This connector-authored marker triggers the exact-commit source audit and V3 runtime final gate after both:
- the backend global-chat RAG isolation fix; and
- the PostgreSQL integration regression assertion locking that boundary.

No test result is claimed by this marker. GitHub Actions execution for this exact commit is authoritative.
