# Global RAG isolation retest

This connector-authored marker triggers exact-commit source and V3 runtime gates after the backend was changed so a GLOBAL chat without selected sources cannot implicitly retrieve from the account-wide Source Library.

No test result is claimed by this marker; GitHub Actions execution is authoritative.
