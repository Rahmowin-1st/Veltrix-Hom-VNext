# Runtime storage contract retest

This connector-authored marker triggers the exact-commit source and V3 runtime gates after `scripts/http_runtime_smoke.py` was aligned with the canonical `S3CompatibleStorageAdapter.id == "s3-compatible"` contract.

No runtime success is claimed by this marker; GitHub Actions execution is authoritative.
